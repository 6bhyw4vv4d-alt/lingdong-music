# AGENTS.md

这个文件是给 AI 编程助手看的项目说明。目标是让助手快速理解项目、少猜测、少返工，并且安全地修改代码。

本项目当前目录暂时没有源码文件；以下内容根据会话 `019e7bce-85dd-7ed3-9403-355061a4b313` 和桌面模板 `/Users/xyx/Desktop/AGENTS.md` 整理。后续如果技术路线或目录结构变化，请同步更新本文件。

## 项目简介

- 项目名称：灵动音乐
- 项目用途：做一个个人使用的 macOS Apple Music 桌面歌词/灵动岛歌词原型。
- 主要用户：项目 owner 自己，优先满足日常听歌时的悬浮歌词体验。
- 当前阶段：v0.2 可安装内测版。
- 核心目标：安装并启动后，在屏幕顶部显示一个可拖动、置顶的灵动岛歌词胶囊，读取 Apple Music 当前歌曲信息和播放进度，从 LRCLIB 匹配同步歌词并显示当前句。

## 技术栈

- 运行平台：macOS。
- 播放器：系统自带 Music.app / Apple Music。
- 第一版语言：JXA（JavaScript for Automation）。
- 系统 UI：AppKit，通过 JXA 的 Objective-C bridge 创建窗口和控件。
- 播放信息读取：AppleScript/JXA 控制 Music.app，读取歌曲名、歌手、专辑、时长、播放进度、播放状态。
- 歌词来源：LRCLIB 在线 API，同步歌词优先。
- 本地数据：JSON 文件保存在 `~/Library/Application Support/灵动音乐/`；项目 `data/` 仅用于开发迁移和格式检查。
- 包管理器：暂无。第一版应尽量依赖 macOS 自带能力，不先引入 npm/pnpm/yarn。
- 测试工具：暂无自动化测试框架。先以脚本试运行、Apple Music 实机流程、JSON 校验为主。
- 已知背景：会话里提到 Node 可用，但 Swift 当前工具链/SDK 不匹配，所以第一版避开 Swift/Xcode。

## 常用命令

请优先使用项目已有命令。如果命令不存在，先查看 `README.md`、`package.json` 或相关配置文件。

```bash
# 启动 JXA 原型（文件创建后使用）
osascript -l JavaScript lyric-island.jxa

# 快速检查 Music.app 当前歌曲读取能力
osascript -e 'tell application "Music" to if it is running then get name of current track'

# 检查人工映射 JSON 是否有效（文件创建后使用）
plutil -lint data/overrides.json

# 构建通用架构 App、DMG 和 SHA-256 校验值
scripts/build-release.sh
```

如果后续改成 Node、Swift、Electron、Tauri 或原生 App，请把上面的命令替换成真实命令。

## 建议目录结构

当前项目目录为空。建议第一版采用轻量结构：

```text
AGENTS.md              # 给 AI 编程助手看的项目说明
README.md              # 给使用者看的安装、启动、权限说明
lyric-island.jxa       # 第一版 JXA + AppKit 主脚本
data/
  overrides.json       # 人工匹配映射，优先本地使用
  cache/               # 可选，LRCLIB 查询结果或歌词缓存
docs/
  notes.md             # 可选，方案记录、限制和后续计划
scripts/
  smoke-test.sh        # 可选，轻量检查脚本
```

不要过早拆分复杂目录。只有当单文件脚本明显难以维护时，再把歌词解析、匹配、缓存、UI 等逻辑拆出去。

## 功能范围

- Apple Music 播放中：显示歌曲名和当前歌词句。
- Music 未运行或未播放：窗口显示等待状态，不报错退出。
- 自动匹配：用歌曲名、歌手、专辑、时长查询 LRCLIB，优先选择同步歌词。
- 匹配排序：加入标题清理、歌手分隔、时长接近度等规则，避免选错版本。
- 匹配失败：仍显示歌曲信息和“未找到同步歌词”，并保留人工补充入口。
- 人工补充：允许修改歌名、歌手、专辑、时长，重新搜索候选歌词。
- 手动指定：允许保存 LRCLIB ID 或粘贴 LRC 文本。
- 本地保存：把人工映射保存到 `~/Library/Application Support/灵动音乐/overrides.json`，优先用 Apple Music track id；取不到时用规范化后的 `artist + title + duration`。
- 播放状态：暂停时停在当前句，继续后恢复更新；换歌后重新匹配。

## 暂不做的事

- 不读取 Apple Music 官方歌词。
- 不要求用户输入 Apple ID。
- 不保存账号、密码、token 或任何登录凭据。
- 第一版不做 App Store 打包、不做完整偏好设置窗口、不做多播放器支持。
- 第一版歌词源只用 LRCLIB；网易云、Spotify、Musixmatch 等来源以后再讨论。
- 不绕过系统权限，不自动修改隐私与安全性设置。

## UI 和前端要求

- 默认窗口是顶部居中的“灵动岛”胶囊。
- 窗口置顶、可拖动，适合在其他 App 前显示。
- 默认视觉：深色半透明背景、圆角胶囊、小号歌名、大号当前歌词。
- 歌词过长时单行省略，不能把窗口撑到失控，也不能遮挡太多屏幕。
- UI 状态要清楚：等待 Music、播放中、暂停、未找到歌词、网络失败、人工补充面板。
- 人工补充面板要小而直接，包含歌名、歌手、专辑、时长、LRCLIB ID 或 LRC 文本，以及重新搜索、保存、取消。
- 不做营销页或介绍页；启动后直接进入可用体验。

## 工作原则

- 先阅读现有代码和数据文件，再开始修改。
- 优先做小而清晰的改动，不做无关重构。
- 保持第一版轻量，优先验证“能读取播放状态、能显示窗口、能匹配歌词”这条主链路。
- 尽量复用 macOS 自带能力，除非确实需要再引入依赖。
- 如果需求不明确，可以做合理假设，但要在最终说明里写清楚。
- 如果会涉及登录、付费 API、系统权限扩大、后台常驻、开机自启、联网上传数据，先询问再改。
- 不要修改和当前任务无关的文件。
- 不要删除用户已有代码或数据，除非任务明确要求。

## 编码规范

- 保持代码简单、可读、容易维护。
- JXA 代码里注意把 Music 读取、LRCLIB 请求、LRC 解析、UI 渲染、人工映射存取分成清楚的函数。
- 命名要清楚，避免无意义缩写。
- 不为了“高级”而增加复杂抽象。
- 只有在 JXA/AppKit 桥接逻辑不容易理解时才添加注释。
- 网络失败、权限失败、Music 未运行等情况必须有兜底显示，不能直接崩溃退出。
- 写 JSON 时保持稳定格式，避免把个人数据和缓存误提交到不该提交的位置。

## 歌词匹配规则

- 优先使用人工映射；命中后直接使用保存的 LRCLIB ID 或 LRC 文本。
- 自动查询 LRCLIB 时，优先使用歌曲名、歌手、专辑、时长。
- 对标题做轻量清理，例如去除括号里的 live/remaster/feat 等噪声时要谨慎，避免误伤中文歌名。
- 对多歌手做分隔处理，但保留原始歌手名作为查询候选。
- 候选结果优先选择同步歌词；没有同步歌词时显示“未找到同步歌词”，不要假装可逐句同步。
- 候选排序要考虑标题相似度、歌手相似度、专辑相似度、时长接近度。
- 用户手动选择过的结果优先级最高。

## 测试要求

- Music 未运行：窗口显示等待状态，不报错退出。
- Music 播放中：能显示当前歌曲信息，换歌后能重新匹配。
- 自动匹配成功：歌词能随播放进度切换。
- 自动匹配失败：出现人工补充入口，不退出、不隐藏。
- 手动改歌名/歌手后重新搜索：能展示候选并选择正确歌词。
- 保存人工映射后：同一首歌再次播放时直接使用保存结果。
- 手动粘贴 LRC：能解析时间戳并随播放进度滚动。
- 暂停/继续：暂停时歌词停在当前句，继续后恢复更新。
- 网络失败：保留窗口并提示歌词服务不可用。
- 权限问题：提示需要允许脚本控制 Music.app，不要无限报错。

## 安全要求

- 不要提交密钥、token、密码、`.env` 文件内容。
- 不要要求用户在脚本里输入 Apple ID 或密码。
- 不要在日志里打印敏感路径或个人账号信息。
- 不要上传用户本地音乐库信息；除查询 LRCLIB 所需的歌曲元数据外，不发送额外数据。
- 不要执行会删除数据的操作，除非用户明确确认。
- 不要使用破坏性 Git 命令，例如 `git reset --hard`，除非用户明确要求。
- 对联网歌词源保持透明：代码里应能看出请求发送到哪里、发送了哪些字段。

## 参考项目和资料

- LyricsX：可参考桌面歌词、菜单栏歌词、歌词偏移、拖拽导入 LRC、歌词缓存等思路。
- LyricFever：可参考 Apple Music 状态读取、播放状态通知、LRCLIB 备用源、离线缓存、背景色等思路。
- Kashi：说明轻量脚本路线可行，可参考 AppleScript 读取当前播放信息再检索歌词的方式。
- LRCGET：LRCLIB 官方客户端，可参考匹配和缓存策略。
- LRCLIB 文档：https://lrclib.net/docs

参考项目只能借鉴思路，不要直接复制大段代码。若引入第三方代码，必须确认许可证并保留必要声明。

## Git 规则

- 修改前可以查看当前改动，但不要回滚用户已有改动。
- 当前项目目录可能尚未初始化 Git；不要擅自初始化仓库，除非用户要求。
- 如果需要提交代码，提交信息尽量简洁清楚。
- 推荐提交信息格式：

```text
feat: add lyric island prototype
fix: handle missing lrclib results
refactor: split lrc parser
test: add music state smoke check
docs: update setup guide
```

## 完成任务后的说明

每次完成修改后，请简要说明：

- 改了什么
- 改了哪些文件
- 做了哪些验证
- 哪些命令没能运行以及原因
- 还有哪些风险或待补充事项

## 给 AI 助手的特别提醒

- 不要只给方案，能直接完成的任务请直接修改代码。
- 遇到报错时，先自己排查常见原因。
- 修改完成后，尽量运行最相关的检查命令。
- 如果某个命令失败，请说明失败原因和下一步建议。
- 保持改动范围小，先解决用户当前问题。
- 本项目的第一优先级是做出可用的个人原型，然后再考虑打包、美化和扩展播放器。
